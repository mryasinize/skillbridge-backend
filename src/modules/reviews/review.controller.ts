import type { NextFunction, Request, Response } from "express";

export const createReviewController = (req: Request, res: Response, next: NextFunction) => {
    try {

    } catch (error) {
        next(error)
    }
};